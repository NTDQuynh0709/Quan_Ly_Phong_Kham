@model web.Models.Patient
@{
    ViewBag.Title = "Tiếp nhận bệnh nhân mới";
    ViewBag.Ctrl = "Patient";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="pg-hd">
    <h4><i class="fas fa-user-plus me-2"></i>Tiếp nhận bệnh nhân mới</h4>
    <a href="@Url.Action("Index", "Patient")" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Quay lại danh sách
    </a>
</div>

<div class="alert alert-info">
    <i class="fas fa-circle-info me-2"></i>
    Dùng chức năng này khi bệnh nhân đến quầy lễ tân lần đầu và cần đăng ký hồ sơ.
    Thông tin bệnh nhân sau khi lưu có thể dùng ngay để tạo lịch khám.
</div>

<div class="card">
    <div class="card-header"><i class="fas fa-id-card me-2 text-primary"></i>Thông tin hành chính</div>
    <div class="card-body">
        @{
            var allErrors = ViewData.ModelState
                .Where(kv => kv.Value.Errors.Count > 0)
                .SelectMany(kv => kv.Value.Errors.Select(e => e.ErrorMessage))
                .Where(m => !string.IsNullOrWhiteSpace(m))
                .Distinct()
                .ToList();
        }
        @if (allErrors.Count > 0)
        {
            <div class="alert alert-danger mb-3" style="border-radius:12px">
                <div style="font-weight:800;margin-bottom:6px">
                    <i class="fas fa-circle-exclamation me-2"></i>Vui lòng kiểm tra lại các thông tin sau:
                </div>
                <ul style="margin:0;padding-left:22px">
                    @foreach (var msg in allErrors)
                    {
                        <li>@msg</li>
                    }
                </ul>
            </div>
        }

        @using (Html.BeginForm("Create", "Patient", FormMethod.Post, new { novalidate = "novalidate" }))
        {
            @Html.AntiForgeryToken()

            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Họ và tên <span style="color:red">*</span></label>
                    @Html.TextBoxFor(m => m.FullName, new {
                        @class = "form-control",
                        placeholder = "Nguyễn Văn A",
                        required = "required",
                        maxlength = "100",
                        pattern = "^[^0-9]+$",
                        title = "Họ tên không được chứa chữ số."
                    })
                    @Html.ValidationMessageFor(m => m.FullName, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-3">
                    <label class="form-label">Ngày sinh</label>
                    @Html.TextBoxFor(m => m.DateOfBirth, "{0:yyyy-MM-dd}", new {
                        @class = "form-control",
                        type = "date",
                        max = DateTime.Today.AddDays(-1).ToString("yyyy-MM-dd"),
                        min = "1900-01-01"
                    })
                    @Html.ValidationMessageFor(m => m.DateOfBirth, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-3">
                    <label class="form-label">Giới tính</label>
                    <select name="Gender" class="form-select">
                        <option value="">Chọn giới tính</option>
                        <option value="Nam" @(Model.Gender == "Nam" ? "selected" : "")>Nam</option>
                        <option value="Nữ" @(Model.Gender == "Nữ" ? "selected" : "")>Nữ</option>
                    </select>
                </div>

                <div class="col-md-6">
                    <label class="form-label">CCCD <span style="color:red">*</span></label>
                    @Html.TextBoxFor(m => m.CCCD, new {
                        @class = "form-control",
                        placeholder = "12 chữ số",
                        required = "required",
                        minlength = "12",
                        maxlength = "12",
                        pattern = "[0-9]{12}",
                        inputmode = "numeric",
                        title = "CCCD phải gồm đúng 12 chữ số."
                    })
                    @Html.ValidationMessageFor(m => m.CCCD, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Số điện thoại <span style="color:red">*</span></label>
                    @Html.TextBoxFor(m => m.Phone, new {
                        @class = "form-control",
                        type = "tel",
                        placeholder = "0912345678",
                        required = "required",
                        minlength = "10",
                        maxlength = "10",
                        pattern = "0[3-9][0-9]{8}",
                        inputmode = "numeric",
                        title = "SĐT phải gồm 10 chữ số, đầu 03/05/07/08/09."
                    })
                    @Html.ValidationMessageFor(m => m.Phone, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Email</label>
                    @Html.TextBoxFor(m => m.Email, new {
                        @class = "form-control",
                        type = "email",
                        placeholder = "email@example.com"
                    })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Địa chỉ</label>
                    @Html.TextBoxFor(m => m.Address, new {
                        @class = "form-control",
                        placeholder = "Nhập địa chỉ liên hệ",
                        maxlength = "255"
                    })
                </div>
            </div>

            <div class="d-flex flex-wrap gap-3 mt-4">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save me-2"></i>Lưu hồ sơ
                </button>
                <a href="@Url.Action("Index", "Patient")" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left me-2"></i>Hủy
                </a>
            </div>
        }
    </div>
</div>
